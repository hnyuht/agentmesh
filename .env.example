# Copy to .env (never commit the real file)

# Browser viewer auth token (plaintext here, only ever transmitted over Tailscale)
VIEWER_TOKEN=change-me-to-a-long-random-string

# Anthropic API key used by every agent's Claude tool-use loop
ANTHROPIC_API_KEY=sk-ant-...
